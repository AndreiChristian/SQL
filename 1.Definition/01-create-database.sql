-- the name of the db should have no white spaces, and it should not clash with
-- reserved keywoards such as create etc. If we want to use such a name, we should 
-- wrap with up ""(most common, including postgre) or ``(for mysql) 

CREATE DATABASE talently;


